# Development Log - 2026-06-24

## 06:30 - My Compass v2 Next.js プロジェクト基盤の実装

- ユーザープロンプト: 前回セッションの引継ぎ。タスク 1.1（Next.js 初期化）〜 2.3（ログイン画面）の自動化可能な部分を実装する
- 実行内容:
  - `package.json`（Next.js 15 + @supabase/ssr + TypeScript）
  - `tsconfig.json`（strict モード + `@/` エイリアス）
  - `tailwind.config.ts`（承認済みデザイントークン: navy/accent/bg/paper 等）
  - `next.config.ts`（セキュリティヘッダー + outputFileTracingRoot）
  - `postcss.config.mjs`、`eslint.config.mjs`
  - `supabase/migrations/0001_initial_schema.sql`（8テーブル + RLS + updated_at トリガー）
  - `src/types/index.ts`（Task/FinanceRecord/WorkShift/DailyReport/CalendarEvent 等 全共有型）
  - `src/lib/supabase/server.ts`・`browser.ts`（createServerClient / createBrowserClient）
  - `src/middleware.ts`（未認証→/login リダイレクト・ALLOWED_EMAIL チェック）
  - `src/app/auth/callback/route.ts`（OAuth コールバック処理）
  - `src/app/globals.css`（Tailwind + カスタムクラス: `.card`, `.metric-value` 等）
  - `src/app/layout.tsx`（ルートレイアウト）
  - `src/app/(auth)/login/page.tsx`（Google ログインボタン + unauthorized エラー表示）
  - `src/components/layout/Sidebar.tsx`（navy背景・斜めテクスチャ・アクティブ左バー・8ナビ項目）
  - `src/components/layout/Header.tsx`（日付表示・ログアウト・ハンバーガー）
  - `src/app/(dashboard)/layout.tsx`（サイドバー + モバイルオーバーレイ + ヘッダー）
  - `src/app/(dashboard)/page.tsx`（ホームダッシュボード: グリッドレイアウト・プレースホルダー）
  - 各ルート stub ページ（tasks/finance/research/work/reports/schedule/apps）
- 検証:
  - `npx tsc --noEmit` → エラーなし
  - `npm run build` → `✓ Compiled successfully` (13ページ生成)
  - CookieOptions 型エラー 14件を @supabase/ssr 型インポートで修正
- 判断・理由:
  - create-next-app を使わず直接ファイル作成（既存 .kiro/docs/developmentLog を保護）
  - Vercel デプロイは Supabase + Google OAuth の手動設定完了後に実施（次ステップ）
- 残課題（手動セットアップ後に実施）:
  - Supabase プロジェクト作成 → SQL エディタで 0001_initial_schema.sql を実行 → SUPABASE_URL/ANON_KEY を .env.local に設定
  - Google Cloud Console で OAuth アプリ登録（callback: [supabase-url]/auth/v1/callback）
  - Supabase Auth > Providers > Google を有効化
  - ALLOWED_EMAIL を .env.local に設定
  - `vercel --prod` または GitHub 連携でデプロイ
