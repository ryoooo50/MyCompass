# 実装計画 — My Compass v2

<!--
🤖 自動実行可能マーク凡例
  [AUTO] クラウドエージェントによる夜間自動実行が可能なタスク
  [MANUAL] Supabase/Google Cloud コンソール操作など手動が必要なタスク
-->

---

- [ ] 1. プロジェクト基盤の構築
- [ ] 1.1 Next.js プロジェクトを初期化し、開発環境を整える `[AUTO]`
  - TypeScript・Tailwind CSS・ESLint・Prettier を含む Next.js 15（App Router）プロジェクトを作成する
  - `src/app`・`src/components`・`src/lib`・`src/types` のディレクトリ構造を設計書の通りに作成する
  - `tsconfig.json` で strict モードを有効化し、パスエイリアス（`@/`）を設定する
  - Tailwind で既存デザイン（白・濃紺）に合わせたカラートークンを定義する
  - _Requirements: すべての要件の基盤_

- [ ] 1.2 Supabase データベーススキーマを作成する `[MANUAL + AUTO]`
  - Supabase プロジェクトを作成し、接続情報を環境変数として整備する（MANUAL）
  - 設計書の 8 テーブル（tasks・finance_records・monthly_budgets・research_phases・research_milestones・work_shifts・daily_reports・user_settings）を SQL マイグレーションファイルとして記述する（AUTO）
  - 全テーブルに RLS を有効化し、`auth.uid() = user_id` ポリシーを適用する（AUTO）
  - `updated_at` を自動更新するトリガー関数を作成する（AUTO）
  - _Requirements: 要件 3 全体_

- [ ] 1.3 Supabase クライアントと TypeScript 型定義を整備する `[AUTO]`
  - サーバーサイド用（Server Components・API Routes 用）と ブラウザ用の Supabase クライアントを別モジュールで実装する
  - `types/index.ts` に設計書で定義した全共有型（Task・FinanceRecord・WorkShift・DailyReport・CalendarEvent 等）を記述する
  - Supabase の DB スキーマから型を自動生成するコマンドを `package.json` スクリプトに追加する
  - _Requirements: 要件 3.1, 3.5_

---

- [ ] 2. 認証システムの実装
- [ ] 2.1 Google OAuth の認証基盤を構築する `[MANUAL + AUTO]`
  - Google Cloud Console で OAuth アプリを登録し、クライアント ID・シークレットを取得する（MANUAL）
  - Supabase Auth の Google プロバイダーを有効化し、コールバック URL を設定する（MANUAL）
  - 環境変数 `ALLOWED_EMAIL` で許可メールアドレスを定義し、Middleware で照合するロジックを実装する（AUTO）
  - _Requirements: 要件 1.1, 1.4_

- [ ] 2.2 認証 Middleware とルートガードを実装する `[AUTO]`
  - Next.js Middleware でセッション Cookie を確認し、未認証リクエストを `/login` にリダイレクトする
  - 認証済みユーザーが `/login` にアクセスした場合は `/` にリダイレクトする
  - matcher で静的アセット（`_next/static`・`_next/image`・`favicon.ico`）を除外する
  - セッション期限切れ時に自動リダイレクトされることを確認する
  - _Requirements: 要件 1.1, 1.2, 1.7_

- [ ] 2.3 ログイン画面を実装する `[AUTO]`
  - 「Google でログイン」ボタンのみを持つシンプルなログインページを作成する
  - OAuth フロー開始・コールバック処理・エラー表示を実装する
  - 許可されていないアカウントでログインした場合のエラーメッセージを表示する
  - ログイン成功後にダッシュボードへリダイレクトする
  - _Requirements: 要件 1.2, 1.3, 1.4_

---

- [ ] 3. ダッシュボードシェルの実装
- [ ] 3.1 サイドバーとヘッダーのレイアウトを実装する `[AUTO]`
  - `(dashboard)/layout.tsx` でサイドバー・ヘッダー・メインコンテンツ領域を構成する
  - サイドバーに全 8 ビュー（ホーム・タスク・財務・研究・アルバイト・日報・予定・アプリ）へのナビゲーションリンクを実装する
  - ヘッダーに現在の日付（日本語表示）とログアウトボタンを配置する
  - ビュー切り替え時にページ上部へスクロールする
  - _Requirements: 要件 2.2, 2.3, 2.4_

- [ ] 3.2 モバイル対応のナビゲーションを実装する `[AUTO]`
  - 768px 未満でサイドバーをハンバーガーメニューに切り替える
  - メニュー開閉のアニメーションと背景オーバーレイを実装する
  - メニュー外クリックで閉じる動作を実装する
  - キーボード操作（Escape キーで閉じる）に対応する
  - _Requirements: 要件 2.4_

- [ ] 3.3 レスポンシブレイアウトを全ビューに適用する `[AUTO]`
  - 1024px 以上でサイドバー常時表示・グリッドレイアウト
  - 768px〜1023px でサイドバー縮小・カード配置調整
  - 768px 未満でモバイルレイアウト・1 列表示
  - 主要なインタラクション要素にキーボードアクセスと `aria-label` を付与する
  - _Requirements: 要件 2.2, 2.3, 2.4, NFR-5_

---

- [ ] 4. タスク管理機能の実装
- [ ] 4.1 タスクの作成・編集・削除を実装する `[AUTO]`
  - タイトル・期限・優先度（高/中/低）・カテゴリを入力できるフォームを実装する
  - フォーム送信で Supabase にレコードを保存し、一覧を即座に更新する（楽観的更新）
  - 既存タスクを編集するインラインフォームまたはモーダルを実装する
  - 削除時に確認ダイアログを表示し、承認後に Supabase から削除する
  - _Requirements: 要件 5.1, 5.3, 5.4_

- [ ] 4.2 タスクの完了切り替えと一覧表示を実装する `[AUTO]`
  - チェックボックスで完了・未完了を切り替え、`completed_at` を記録する
  - 楽観的更新で即座に UI を反映し、Supabase への保存失敗時にロールバックする
  - 優先度・期限・カテゴリでフィルタリングとソートができる UI を実装する
  - 期限切れ（期日が今日以前で未完了）のタスクを視覚的に区別して表示する
  - _Requirements: 要件 5.2, 5.5, 5.6_

---

- [ ] 5. Google Calendar 連携の実装
- [ ] 5.1 Google Calendar API プロキシを実装する `[AUTO]`
  - `/api/calendar/events` ルートでセッションから Google OAuth トークンを取得し、Google Calendar API を呼び出す
  - `/api/calendar/calendars` ルートでユーザーのカレンダー一覧を取得する
  - Access Token の期限切れを検知してリフレッシュし、失敗時は 502 エラーを返す
  - レスポンスを `CalendarEvent[]` 型に整形して返す
  - _Requirements: 要件 9.1, 9.2, 9.4, 9.8_

- [ ] 5.2 カレンダー選択と設定を実装する `[AUTO]`
  - Google Calendar の同意画面で `calendar.readonly` スコープを追加要求する（初回ログイン時）
  - 取得したカレンダー一覧から表示するカレンダーを選択できる設定 UI を実装する
  - 選択したカレンダー ID を `user_settings.calendar_ids` に保存する
  - _Requirements: 要件 9.1, 9.7_

- [ ] 5.3 予定ウィジェットを実装する `[AUTO]`
  - 今後 7 日間の予定を曜日別に表示するウィジェットを実装する
  - 取得中にローディングインジケータを表示する
  - 取得失敗時にエラー状態と最終取得データを表示する
  - 手動更新ボタンで再取得できる
  - _Requirements: 要件 9.3, 9.5, 9.6_

---

- [ ] 6. ホームダッシュボードの実装
- [ ] 6.1 指標カードを実装する `[AUTO]`
  - 今月の残額（財務データから計算）・今日の未完了タスク数・研究全体進捗率・今月の勤務時間を指標カードで表示する
  - 当日の日報が未記入の場合に記入を促すカードを表示する
  - 各カードをクリックすると対応するビューに遷移する
  - _Requirements: 要件 4.1, 4.4, 12.5_

- [ ] 6.2 ホームの今日のタスクウィジェットを実装する `[AUTO]`
  - 本日期限または優先度「高」のタスクを最大 6 件、優先度順に表示する
  - チェックボックスでホームから直接完了切り替えができる
  - 「タスクを全部見る」リンクでタスクビューに遷移する
  - _Requirements: 要件 4.2, 4.3_

- [ ] 6.3 ホームの週間予定ウィジェットを実装する `[AUTO]`
  - Google Calendar から取得した今週の予定を曜日別に一覧表示する
  - 取得中・エラー・空の各状態を適切に表示する
  - _Requirements: 要件 4.3, 4.5, 4.6_

---

- [ ] 7. 財務管理機能の実装
- [ ] 7.1 収支レコードの作成・編集・削除を実装する `[AUTO]`
  - 金額・カテゴリ（選択式）・種別（収入/支出）・日付・メモを入力できるフォームを実装する
  - 既存レコードの編集・削除（確認付き）を実装する
  - 変更後に月次集計を再計算して表示を更新する
  - _Requirements: 要件 6.1, 6.4_

- [ ] 7.2 月次サマリーとグラフを実装する `[AUTO]`
  - 当月の収入合計・支出合計・残額・貯蓄率を計算して表示する
  - 直近 6 ヶ月の収入・支出推移を棒グラフで表示する（Recharts または Chart.js）
  - _Requirements: 要件 6.2, 6.3_

- [ ] 7.3 予算管理と警告表示を実装する `[AUTO]`
  - 月次予算を設定する入力フォームを実装する
  - 支出が予算を超えた場合にホームの指標カードに警告バッジを表示する
  - _Requirements: 要件 6.6_

- [ ] 7.4 収支データの CSV エクスポートを実装する `[AUTO]`
  - 期間を指定して収支レコードを CSV 形式でダウンロードする機能を実装する
  - ブラウザで直接ダウンロードが始まるよう Blob URL を生成する
  - _Requirements: 要件 6.5_

---

- [ ] 8. 研究管理機能の実装
- [ ] 8.1 フェーズ別進捗の表示と更新を実装する `[AUTO]`
  - 文献レビュー・データ収集・分析・論文執筆の 4 フェーズの進捗率をスライダーまたは数値入力で更新できる
  - 全体進捗率を各フェーズ進捗の平均として自動計算して表示する
  - _Requirements: 要件 7.1, 7.2_

- [ ] 8.2 マイルストーン管理を実装する `[AUTO]`
  - マイルストーンのタイトル・期日・達成状態を管理する CRUD を実装する
  - 期日が今日以前で未達成のマイルストーンを期限切れとして視覚的に区別する
  - _Requirements: 要件 7.3, 7.5_

- [ ] 8.3 Obsidian URI リンクを実装する `[AUTO]`
  - `user_settings.obsidian_vault_name` の設定値から `obsidian://open?vault=...` URI を生成し、リンクとして表示する
  - Vault 名が未設定の場合は設定を促すメッセージを表示する
  - _Requirements: 要件 7.4_

---

- [ ] 9. アルバイト管理機能の実装
- [ ] 9.1 シフト登録・管理を実装する `[AUTO]`
  - 日付・開始時刻・終了時刻・時給・交通費を入力できるシフト登録フォームを実装する
  - 登録済みシフトの一覧表示・編集・削除を実装する
  - _Requirements: 要件 8.1, 8.4_

- [ ] 9.2 勤務時間と給与見込みの計算を実装する `[AUTO]`
  - 当月のシフト一覧から累計勤務時間を計算して表示する
  - 給与見込み（時給 × 勤務時間 + 交通費合計）を計算して表示する
  - _Requirements: 要件 8.2_

- [ ] 9.3 確定済みシフトを財務データに連動させる `[AUTO]`
  - シフトを「確定」にすると `finance_records` の収入レコードとして自動登録する
  - 確定を取り消した場合は対応する収入レコードも削除する
  - _Requirements: 要件 8.3_

---

- [ ] 10. 日報機能の実装
- [ ] 10.1 日報の作成・編集を実装する `[AUTO]`
  - 「今日やったこと」「気づき・メモ」「明日やること」の 3 フィールドを持つ日報フォームを実装する
  - 当日の日報が既存の場合は既存内容を表示し、追記・上書きできる
  - フォーム送信で `daily_reports` テーブルに upsert する（`user_id + report_date` でユニーク）
  - _Requirements: 要件 12.1, 12.2, 12.3_

- [ ] 10.2 過去の日報の閲覧と一覧を実装する `[AUTO]`
  - 日付ピッカーで過去の日付を選択し、その日の日報を表示する
  - 過去の日報を新しい順に一覧表示するページを実装する
  - _Requirements: 要件 12.4, 12.6_

- [ ] 10.3 日報の記入状況をホームに連携する `[AUTO]`
  - ホームの指標カードに当日の日報が記入済みかどうかを表示する
  - 未記入の場合は記入を促すリンク付きの通知をホームに表示する
  - _Requirements: 要件 12.5, 12.7_

---

- [ ] 11. アプリランチャーと設定の実装
- [ ] 11.1 標準アプリリンクと設定保存を実装する `[AUTO]`
  - Obsidian・Notion・Google Calendar・Google Drive・GitHub の固定リンクを表示する
  - Obsidian Vault 名・Notion ホーム URL の入力・保存・復元を `user_settings` テーブルで管理する
  - Obsidian Vault 名から `obsidian://open?vault=...` URI を動的に生成する
  - _Requirements: 要件 11.1, 11.2_

- [ ] 11.2 カスタム URL の登録とランチャー表示を実装する `[AUTO]`
  - 任意のラベルと URL を入力して登録できるフォームを実装する
  - `https:`・`http:`・`obsidian:` のみを許可する URL 形式検証を実装する
  - 登録したカスタムアプリをランチャーに追加表示する
  - 外部リンクは `rel="noreferrer noopener"` を付与した新しいタブで開く
  - _Requirements: 要件 11.3, 11.4, 11.5_

---

- [ ] 12. セキュリティ強化
- [ ] 12.1 セキュリティヘッダーと入力検証を実装する `[AUTO]`
  - `next.config.ts` でセキュリティヘッダー（X-Frame-Options・X-Content-Type-Options・Referrer-Policy）を設定する
  - URL 入力フィールドで `URL` コンストラクタによる形式検証を実装し、許可スキーム以外を拒否する
  - API Route で未認証リクエストに 401 を返すガードを全ルートに適用する
  - _Requirements: 要件 13.1, 13.2, 13.4, 13.5_

- [ ] 12.2 エラーハンドリングとトースト通知を実装する `[AUTO]`
  - Supabase への保存失敗時に楽観的更新をロールバックし、トースト通知を表示する
  - Google Calendar 取得失敗時にエラー状態を表示し、最終取得データを維持する
  - ネットワーク切断時にオフラインバナーを表示する
  - _Requirements: 要件 3.3, 9.4, 9.6, NFR-2_

---

- [ ] 13. テストの実装
- [ ] 13.1 ユニットテストを実装する `[AUTO]`
  - Vitest を導入し、給与計算・進捗率計算・日付フォーマット等のユーティリティ関数を検証するテストを記述する
  - Google Calendar API レスポンスの整形ロジックをテストする
  - URL 検証ロジックをテストする
  - _Requirements: NFR-6, 要件 11.5_

- [ ] 13.2 インテグレーションテストを実装する `[AUTO]`
  - MSW（Mock Service Worker）を導入し、`/api/calendar/events` の正常系・エラー系・トークンリフレッシュを検証する
  - Supabase のモックで RLS ポリシーのユニット検証を実施する
  - Middleware の認証済み・未認証それぞれのリダイレクト動作を検証する
  - _Requirements: 要件 1.1, 1.2, 9.4, 9.8_

- [ ] 13.3 E2E テストを実装する `[AUTO]`
  - Playwright を導入し、認証フロー（Google OAuth → ダッシュボード）をテストアカウントで検証する
  - タスクの追加 → 完了切り替え → 削除の一連操作を検証する
  - 日報の作成 → 保存 → 再読み込みで内容が維持されることを検証する
  - 320px・768px・1440px の各幅でレイアウト崩れがないことをスクリーンショットで検証する
  - _Requirements: 要件 1, 5, 12, NFR-4_

---

- [ ] 14. PWA 対応とパフォーマンス最適化
- [ ] 14.1 PWA を実装する `[AUTO]`
  - `manifest.json` にアプリ名・アイコン・テーマカラーを設定する
  - Service Worker で静的アセットと最後に取得した API レスポンスをキャッシュする
  - オフライン時にキャッシュデータを表示し、オフライン状態をバナーで明示する
  - _Requirements: 要件 2.5, 2.6_

- [ ] 14.2 Web Push 通知を実装する `[AUTO]`
  - Supabase に `notifications` テーブルを追加し、タイトル・本文・既読フラグを管理する
  - VAPID キーを生成し、ユーザーの Push 購読情報を Supabase に保存する
  - Service Worker で Push イベントを受信し、OS ネイティブ通知を表示する
  - `/api/notify` ルートを実装し、Supabase への書き込みをトリガーに Push を送信する
  - クラウドエージェント（夜間実装ルーティン等）が Supabase 経由で通知を送れるようにする
  - 通知許可が未付与の場合は許可リクエストを促す UI を実装する
  - _Requirements: NFR-6_

- [ ] 14.3 パフォーマンスを最適化する `[AUTO]`
  - 画像に `width`・`height` と適切な `loading` 属性を設定する
  - 初期ページ表示が 3 秒以内に収まることを Lighthouse で確認する
  - グラフライブラリを動的インポートで遅延読み込みする
  - _Requirements: NFR-1_

---

- [ ] 15. Notion 連携（Phase 2）
- [ ] 15.1 Notion Integration Token の管理を実装する `[AUTO]`
  - 設定画面で Notion Integration Token を入力・保存する UI を実装する
  - トークンは Vercel 環境変数として保存し、フロントエンドに送信しない設計を実装する
  - _Requirements: 要件 10.1_

- [ ] 15.2 Notion タスク同期 API Route を実装する `[AUTO]`
  - `/api/notion/sync` ルートで指定した Notion データベースからタスクを取得する
  - `/api/notion/tasks/[id]` ルートで Notion のレコードに完了状態を書き戻す
  - 同期失敗時は Supabase のデータを維持し、エラーをトーストで通知する
  - _Requirements: 要件 10.2, 10.3, 10.4_

- [ ] 15.3 Notion との競合解決 UI を実装する `[AUTO]`
  - My Compass と Notion の両方で変更があった場合に差分を表示する
  - どちらの変更を優先するかをユーザーが選択できるダイアログを実装する
  - _Requirements: 要件 10.5_
